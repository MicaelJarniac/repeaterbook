"""Optional MCP server exposing RepeaterBook to agents.

Requires the ``mcp`` extra. To run the server, point an MCP client at
``uvx --from "repeaterbook[mcp]" repeaterbook-mcp``; to depend on this
subpackage as a library, ``uv add "repeaterbook[mcp]"``.
"""
